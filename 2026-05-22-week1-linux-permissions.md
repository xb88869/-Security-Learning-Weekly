---
title: "第1周：Linux 文件系统与权限——从"一切皆文件"到 SSH 后门"
date: 2026-05-22
description: "学习 Linux 权限模型的第一周，理解 UGO、rwx、SUID 以及为什么 authorized_keys 是攻击者的首要目标。"
tags: [Linux, 安全, 权限, SSH]
---

## 一、"一切皆文件"——Linux 的世界观

Linux 最核心的设计哲学就是：**一切皆文件**。

- 普通文件是文件
- 目录是文件（记录了文件名列表的文件）
- 设备是文件（`/dev/sda`）
- 网络连接也是文件（socket）
- 命令也是可执行文件

唯一的例外是 **Shell 内建命令**（如 `cd`、`echo`），它们的代码直接在 Bash 程序里，不在磁盘上。

**安全上的两层含义**：

第一层——内建命令不在磁盘上，常规的文件完整性检查验不到它。如果 `/bin/bash` 被替换，所有内建命令都不可信。

第二层——反过来，系统被植入 rootkit、`/bin/ls` 和 `/bin/ps` 都是假的时候，`echo`、`cd`、`type` 这些内建命令仍然可信，可以用来排查。当然，如果 bash 本身也被换了，那只能 Live USB 挂载磁盘检查。

## 二、家目录里藏了什么？

`ls -la ~` 看到的隐藏文件，每一个都有安全含义：

| 文件 | 用途 | 安全关注点 |
|------|------|-----------|
| `.bashrc` | 每次打开终端执行的配置 | 攻击者加恶意别名劫持 sudo |
| `.profile` | 登录时执行的配置 | 更隐蔽的后门植入点 |
| `.bash_history` | 命令历史记录 | 应急响应第一现场，攻击者会清理 |
| `.ssh/` | SSH 密钥与授权 | 被篡改等于服务器沦陷 |

一个值得注意的细节：SSH 登录时两个文件都会执行，但这不是 bash 自动做的——是 `.profile` 末尾主动 source 了 `.bashrc`。如果攻击者删掉这段调用，`.bashrc` 就不会跑了。

## 三、权限模型——UGO 与 rwx

### 三种身份

| 身份 | 简称 | 说明 |
|------|------|------|
| User | u | 文件所有者 |
| Group | g | 所属组成员 |
| Others | o | 其他人 |

### 三种权限——对文件和目录完全不同

| 权限 | 对文件 | 对目录 |
|------|--------|--------|
| r (4) | 查看内容 | 列出文件名（ls） |
| w (2) | 修改内容 | 创建/删除文件 |
| x (1) | 当作程序执行 | 进入目录（cd） |

### 实战解读：`-rwxr-x---`

```
- rwx r-x ---
│  │   │   └─ 其他人：无权限
│  │   └──── 所属组：读 + 执行
│  └──────── 所有者：读 + 写 + 执行
└────────── 普通文件
```

数字法：r=4, w=2, x=1。`rwx`=7, `r-x`=5, `---`=0 → **750**

常见权限速记：
- 755 — 经典程序/目录权限
- 644 — 经典文件权限
- 700 — 仅所有者能碰
- 600 — 仅所有者能读写

**关键认知**：对目录有 w 就能删除里面的文件，不管文件本身的权限是什么。家目录如果是 777，等于开门揖盗。

## 四、特殊权限——SUID / SGID / Sticky Bit

| 特殊权限 | 数字 | 对文件 | 对目录 | 安全风险 |
|----------|------|--------|--------|----------|
| SUID | 4xxx | 以文件所有者身份执行 | 无意义 | 提权攻击重灾区 |
| SGID | 2xxx | 以文件所属组身份执行 | 新建文件继承目录的组 | 权限横向扩散 |
| Sticky Bit | 1xxx | 无意义 | 只有所有者能删自己的文件 | 保护 /tmp |

### SUID 为什么是安全重灾区

`/usr/bin/passwd` 是最经典的 SUID 程序——所有者是 root，设了 SUID 位（`-rwsr-xr-x`），普通用户执行时临时获得 root 身份去写 `/etc/shadow`，写完退出。

攻击者拿到低权限 shell 后，第一件事就是：

```bash
find / -perm -4000 -type f 2>/dev/null
```

列出所有 SUID 文件，逐个找漏洞。任何一个有缓冲区溢出、命令注入等漏洞的 SUID 程序，都可能被利用提权到 root。

### Sticky Bit 保护 /tmp

`/tmp` 权限是 `drwxrwxrwt`——所有人可写，但最后一位 `t` 保证了：只有文件所有者才能删除自己的文件。

## 五、SSH 免密登录与后门原理

### 免密登录的原理

1. 你的私钥留在本地
2. 服务器的 `~/.ssh/authorized_keys` 存着你的公钥
3. 服务器用公钥加密一段随机数发给你，你用私钥解开返回
4. 服务器确认你能解开 = 你就是钥匙的主人

### 配置过程

```bash
ssh-keygen -t ed25519                     # 生成密钥对
ssh-copy-id 用户名@服务器IP                # 部署公钥到服务器
ssh 用户名@服务器IP                        # 验证免密登录
```

### 攻击者视角

如果攻击者往你的 `authorized_keys` 里追加一行他自己的公钥，他就能随时以你的身份免密登录这台机器。这就是最经典的 **SSH 后门**。

这就是为什么：
- `.ssh/` 目录必须是 **700**（`drwx------`）
- `authorized_keys` 文件必须是 **600**（`-rw-------`）

## 六、用户隔离：sudo 不是万能钥匙

```bash
sudo useradd -m testuser     # 创建新用户
sudo passwd testuser          # 设置密码
su - testuser                 # 切换过去
sudo ls /root                 # 失败：不在 sudoers 中
exit                          # 退回
```

普通用户即使登录了系统，没有 sudo 权限也是**囚徒**。这是 Linux 安全的基本盘：**权限与用户强绑定，不授权，无操作。**

## 七、本周安全审计清单

每周检查家目录，把这 4 个点过一遍：

1. `.ssh/` 目录权限是否为 **700**
2. `authorized_keys` 文件权限是否为 **600**，内容是否有未知公钥
3. `.bashrc` 文件权限是否为 **644**，是否有可疑别名
4. `.bash_history` 是否有异常命令（或被人清空过）

---

*欢迎 Star 和 Issue 讨论。这是我的安全学习周记系列的第 1 篇。*
