# 第1周：Linux 文件系统与权限模型

> **目标**：建立"一切皆文件"的心智模型，理解权限是安全的第一道闸门。
> **周期**：5月11日（周一）— 5月16日（周六） → 5月17日（周日）发博客

---

## 每日任务清单

### Day 1（周一）| 安家与认路

**目标**：熟悉家目录和基本移动，理解 Shell 是什么。

**动手清单：**
- [x] `pwd` 看自己在哪，理解 `/home/用户名` = `~`
- [x] `ls -la ~` 列出所有文件，观察 `.bashrc`、`.profile`、`.ssh/`
- [x] `cd /etc` 逛系统配置目录，`cd -` 回家
- [x] `man hier` 读前三屏，了解 `/etc`、`/var`、`/tmp`、`/home` 的职责

**理解要点：**
- Shell 是什么（命令解释器，本质也是文件）
- 内建命令 vs 外部命令（`type` 区分）
- `$` 提示符 = 普通用户，`#` = root

**记录产出：**

- 写下你的用户名、家目录路径
- 记下 `/etc`、`/var`、`/tmp` 各是干什么的

---

### Day 2（周二）| 文件操作基本功

**目标**：掌握文件的增删改查，学会用 `man`。

**动手清单：**

- [x] `mkdir -p ~/week1-lab/subdir` 创建多层目录
- [x] `touch test.txt` 创建空文件，`echo "Hello" > hello.txt` 写入内容
- [x] `cp hello.txt hello.bak` 备份，`mv hello.txt subdir/` 移动
- [x] `rm test.txt` 删除（删除前先 `ls` 确认）
- [x] `man cp` 搜索 `-i`（`/ -i`），理解交互式保护
- [x] `alias rm='rm -i'` 设置别名防手抖

**理解要点：**
- Linux 下一切皆文件：普通文件、目录、设备、socket
- `man` 的用法：`/搜索关键词`、`q` 退出、`n/N` 上下翻匹配

**记录产出：**
- 在 `~/week1-lab` 下完成所有操作，截图或记下输出

---

### Day 3（周三）| 权限模型——UGO 与 rwx

**目标**：彻底搞懂 `rwx` 对文件和目录的不同含义。

**动手清单：**
- [x] `ls -ld ~/.ssh` 看目录权限，理解为什么是 `700`
- [x] `stat ~/.ssh/authorized_keys` 看文件权限，理解为什么是 `600`
- [x] 创建测试文件：`touch ~/week1-lab/test`，用 `chmod 644`/`700`/`777` 反复改权限
- [x] `chmod u+x ~/week1-lab/test` 用符号法加执行权限
- [x] 尝试对目录去掉 `x` 权限，看 `cd` 还能不能进去

**理解要点：**

| 权限 | 对文件 | 对目录 |
|------|--------|--------|
| `r` | 查看内容（cat/less） | 列出文件名（ls） |
| `w` | 修改内容 | 创建/删除文件 |
| `x` | 当作程序执行 | 进入目录（cd） |

- UGO 三重身份：User（所有者）、Group（所属组）、Others（其他人）
- 数字法：`r=4` `w=2` `x=1` → `rwxr-x---` = `750`
- **安全视角**：家目录如果是 `777`，等于开门揖盗

**记录产出：**
- 能口述 `-rwxr-x---` 对所有者、组、其他人分别意味着什么（本周核心产出）

---

### Day 4（周四）| 休息日——轻量回顾

- [x] 不看笔记，口述 UGO 和 rwx 的含义
- [x] 口述 Linux 下"一切皆文件"的意思
- [x] 思考：`.bashrc` 和 `.profile` 有什么区别？

---

### Day 5（周五）| 特殊权限与 SSH 安全（核心）

**目标**：理解 SUID/SGID/Sticky Bit，亲手配置 SSH 免密登录。

**动手清单：**
- [x] `ls -ld /tmp` 确认黏滞位（`drwxrwxrwt` 中的 `t`）
- [x] `ls -l /usr/bin/passwd` 确认 SUID（`-rwsr-xr-x` 中的 `s`）
- [x] `find / -perm -4000 -type f 2>/dev/null` 审计系统所有 SUID 文件
- [x] `ssh-keygen -t ed25519` 生成密钥对
- [x] `ssh-copy-id 用户名@虚拟机IP` 将公钥部署到虚拟机
- [x] 验证免密登录：`ssh 用户名@虚拟机IP`

**理解要点：**

| 特殊权限 | 数字 | 作用 | 风险 |
|----------|------|------|------|
| SUID | `4xxx` | 以文件所有者身份执行 | 提权攻击重灾区 |
| SGID | `2xxx` | 以文件所属组身份执行 | 权限扩散 |
| Sticky Bit | `1xxx` | 仅所有者能删自己的文件 | 保护 `/tmp` |

- **安全思考**：如果攻击者往你的 `authorized_keys` 里加一行他的公钥，他就能随时免密登录你的服务器——这就是最经典的 SSH 后门。

**记录产出：**
- 截下免密登录成功的画面
- 记录你系统里有哪些 SUID 文件

---

### Day 6（周六）| 配置文件安全审计

**目标**：理解 `.bashrc`、`.profile`、`.bash_history` 的安全含义。

**动手清单：**
- [x] `cat ~/.bashrc` 读一遍自己的配置，看有没有可疑别名
- [x] `cat ~/.profile` 看环境变量设置
- [x] `tail -20 ~/.bash_history` 看最近命令历史
- [x] `ls -l ~/.bashrc ~/.profile ~/.bash_history` 确认权限
- [x] 新建一个用户：`sudo useradd -m testuser && sudo passwd testuser`
- [x] `su - testuser` 切换到新用户，尝试 `sudo ls /root` **失败**
- [x] `exit` 退回，理解权限隔离的含义

**理解要点：**
- `.bashrc`：每次打开终端执行，攻击者的持久化后门最爱
- `.profile`：登录 Shell 执行一次，更隐蔽的植入点
- `.bash_history`：应急响应的第一现场，攻击者会清理
- `sudo` 不是万能钥匙——用户不在 `sudoers` 中就是囚徒

**记录产出：**
- 列出一份"家目录安全检查清单"（至少 4 个检查点）

---

### Day 7（周日）| 复习 + 写博客

**目标**：串联所有知识点，输出本周博客到 GitHub。

**知识串联：**
- [ ] 文件权限：`rwx` + UGO + 特殊权限（SUID/SGID/Sticky Bit）
- [ ] 家目录安全：`.ssh/`（700）、`authorized_keys`（600）、`.bashrc`（644）
- [ ] SSH 后门原理：`authorized_keys` 被篡改 = 服务器沦陷
- [ ] 用户隔离：普通用户没有 sudo 权限就是囚徒

**博客写作：**
- [ ] 使用下方的博客模板，完成本周第一篇博客
- [ ] 推送到 GitHub 仓库

---

## 每周产出

**本周核心产出（Github 博客发布）：**

1. 一篇技术博客 —— Linux 文件系统与权限入门（含动手记录 + 安全思考）
2. 能口述 `-rwxr-x---` 对所有者、组、其他人分别意味着什么
3. 已实现 SSH 免密登录虚拟机
4. 已审计家目录安全基线（`.ssh`、`.bashrc`、`.profile`、`.bash_history`）

**博客发布节奏：**
- 每周四发布上一篇的博客
- 本周博客于 **5月17日（周六）** 发布

---

## GitHub 博客模板（可直接使用）

```markdown
---
title: "第1周：Linux 文件系统与权限——从"一切皆文件"到 SSH 后门"
date: 2026-05-22
description: "学习 Linux 权限模型的第一周，理解 UGO、rwx、SUID 以及为什么 authorized_keys 是攻击者的首要目标。"
tags: [Linux, 安全, 权限, SSH]
---

## 前言

这个系列是我从零开始学习网络安全运维的周记。每周学一个主题，周末整理成博客，发在 GitHub 上。既是自己的学习记录，也希望给同样在入门路上的朋友一些参考。

第1周的主题是 **Linux 文件系统与权限模型**。

---

## 一、"一切皆文件"——Linux 的世界观

Linux 最核心的设计哲学就是：**一切皆文件**。

- 普通文件是文件
- 目录是文件（记录了文件名列表的文件）
- 设备是文件（`/dev/sda`）
- 网络连接也是文件（socket）
- 命令也是可执行文件

唯一的例外是 **Shell 内建命令**（如 `cd`、`echo`），它们的代码直接在 Bash 程序里，不需要去磁盘上找。

> **安全思考**：因为内建命令不是独立文件，无法通过文件完整性检查（如 `rpm -V`）来验证。如果 `/bin/bash` 本身被替换了，所有内建命令都不可信。

### 命令提示符的安全含义

- `$` —— 普通用户
- `#` —— root 用户

> 每次 `sudo su -` 干完活，记得 `exit` 退回来。用 root  Shell 操作，一个误 rm 就是灾难。

---

## 二、家目录里藏了什么？

`ls -la ~` 看到的隐藏文件，每一个都有安全含义：

| 文件 | 用途 | 安全关注点 |
|------|------|-----------|
| `.bashrc` | 每次打开终端执行的配置 | 攻击者加恶意别名劫持 sudo |
| `.profile` | 登录时执行的配置 | 更隐蔽的后门植入点 |
| `.bash_history` | 命令历史记录 | 应急响应第一现场 |
| `.ssh/` | SSH 密钥与授权 | 被篡改等于服务器沦陷 |

---

## 三、权限模型——UGO 与 rwx

### 三种身份

| 身份 | 简称 | 说明 |
|------|------|------|
| User | u | 文件所有者 |
| Group | g | 所属组成员 |
| Others | o | 其他人 |

### 三种权限——对文件和目录完全不同

| 权限 | 对文件 | 对目录 |
|------|--------|--------|
| r (4) | 查看内容 | 列出文件名 |
| w (2) | 修改内容 | 创建/删除文件 |
| x (1) | 当作程序执行 | 进入目录（cd） |

> **关键点**：对目录只有 r 没有 x，你能看到文件名但进不去。对目录有 w，你**能删除里面的文件**，不管文件本身的权限是什么。

### 实战：解读 `-rwxr-x---`

```
- rwx r-x ---
│ │    │   └─ 其他人：无权限
│ │    └──── 所属组：读 + 执行
│ └──────── 所有者：读 + 写 + 执行
└────────── 普通文件（d 的话就是目录）
```

数字法：`rwx` = 7，`r-x` = 5，`---` = 0 → **750**

### 特殊权限（安全重点）

```
find / -perm -4000 -type f 2>/dev/null
```

这条命令列出系统里所有 **SUID** 文件。为什么要查？

| 特殊权限 | 作用 | 被攻击者利用的方式 |
|----------|------|-------------------|
| SUID (4) | 以所有者身份执行程序 | 利用有 SUID 的程序提权到 root |
| SGID (2) | 以所属组身份执行程序 | 权限横向扩散 |
| Sticky Bit (1) | 仅能删除自己的文件 | 保护 /tmp，防止删别人文件 |

---

## 四、SSH 免密登录与后门原理

### 配置过程

```bash
# 1. 生成密钥对（推荐 ed25519，比 RSA 更安全）
ssh-keygen -t ed25519

# 2. 将公钥部署到服务器
ssh-copy-id 用户名@服务器IP

# 3. 验证免密登录
ssh 用户名@服务器IP
```

### 安全思考：为什么 authorized_keys 必须严加看管？

**免密登录的原理是**：
1. 你的私钥（钥匙）留在本地
2. 服务器的 `~/.ssh/authorized_keys` 存着你的公钥（锁）
3. 服务器用公钥加密一段随机数发给你，你用私钥解开返回
4. 服务器确认你能解开 = 你就是钥匙的主人

**攻击者视角**：
> 如果他能在 `authorized_keys` 里追加一行他自己的公钥，他就能随时以你的身份免密登录这台机器。

这就是最经典的 **SSH 后门**。这也是为什么：
- `.ssh/` 目录必须是 `700`（`drwx------`）
- `authorized_keys` 文件必须是 `600`（`-rw-------`）

---

## 五、用户隔离：sudo 不是万能钥匙

```bash
sudo useradd -m testuser   # 创建新用户
sudo passwd testuser        # 设置密码
su - testuser               # 切换过去
sudo ls /root               # 失败：不在 sudoers 中
```

普通用户即使登录了系统，没有 sudo 权限也是**囚徒**。这是 Linux 安全的基本盘：**权限与用户强绑定，不授权，无操作**。

---

## 六、本周安全审计清单

每周检查你的家目录，把这 4 个点过一遍：

1. `.ssh/` 目录权限是否为 **700**
2. `authorized_keys` 文件权限是否为 **600**，内容是否有未知公钥
3. `.bashrc` 文件权限是否为 **644**，是否有可疑别名或命令
4. `.bash_history` 是否有异常命令（或被人清空过）

---

## 下周预告

第2周：**用户管理、进程与三次握手**
- Linux 用户/组管理，`ps aux` 识别异常进程
- Wireshark 抓包，认出三次握手的 SYN/SYN-ACK/ACK

---

*欢迎 Star 和 Issue 讨论。这是 [我的安全学习周记系列] 的第1篇。*
```

---

## 发布指引

1. 在 GitHub 上创建一个仓库（如 `Security-Learning-Weekly`）
2. 在仓库下建 `_posts/` 或直接 `posts/` 目录
3. 把博客内容保存为 `2026-05-22-week1-linux-permissions.md`
4. 如果使用 GitHub Pages + Jekyll，把 `---` 之间的内容适配为 Jekyll 的 front matter
5. 或者直接放到仓库根目录的 `README.md`，简单粗暴

**后续每周**：按此模板更新日期、周数、内容即可，第8周时可以整理一个总目录页。
