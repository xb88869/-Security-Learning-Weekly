# 5月到8月计划（优化版）

> **背景**：网络空间安全大一学生，目标本科毕业直接就业，岗位方向：安全运维工程师。
> **优化原则**：确保可完成 > 追求覆盖面。砍掉约 20% 超纲内容，补入面试刚需，加入缓冲周。
> **前置要求**：电脑内存 ≥ 16GB（建议 32GB），已安装 VirtualBox/VMware，可创建至少 3 台虚拟机。

---

## 一、学期阶段（5月5日–7月12日，共10周）

### 每日节奏

- 周一到周五：晚上 1.5–2 小时动手
- 周六：3 小时集中实验
- 周日：1 小时复盘（回顾笔记、整理本周收获、更新 GitHub）

---

### 第1周：Linux 文件系统与权限模型

**目标**：建立"一切皆文件"的心智模型，理解权限是安全的第一道闸门。

**动手清单**：
1. `pwd`、`ls -la ~`、`cd /etc && cd -`——认识家目录与系统目录
2. `man hier` 读前三屏，了解 `/etc`、`/var`、`/tmp`、`/home` 的职责
3. 用 `stat` 查看 `.ssh` 目录权限，理解为什么必须是 700（`drwx------`）
4. 生成 SSH 密钥对（`ssh-keygen`），将公钥放入虚拟机 `authorized_keys`，实现免密登录
5. **安全思考**：如果攻击者往 `authorized_keys` 里追加一行他的公钥，会发生什么？

**重点理解（而非死记）**：
- `r`/`w`/`x` 对文件和对目录的含义完全不同
- SUID、SGID、Sticky Bit 三个特殊权限——这是提权攻击的基础知识
- `.bashrc`、`.profile`、`.bash_history`——攻击者的持久化后门最爱

**产出**：能口述"一个文件的权限 `-rwxr-x---` 对所有者、组、其他人分别意味着什么"。

---

### 第2周：用户管理、进程与文本三剑客入门

**目标**：理解多用户隔离，学会看进程，迈出日志处理第一步。

**动手清单**：
1. `useradd -m testuser` + `passwd testuser` + `su - testuser`——创建一个受限用户
2. 尝试 `sudo ls /root` 观察被拒绝——理解 sudoers 机制
3. `ps aux` 浏览进程列表，用 `ls -l /proc/<PID>/exe` 查看进程的可执行文件路径
4. `ps aux | grep ssh` 初体验管道
5. `grep` 基础：在 `/var/log/auth.log` 中搜索 "Failed password"

**重点理解**：
- 用 `lastb` 读 `/var/log/btmp`（失败登录记录），用 `last` 读 `/var/log/wtmp`（所有登录记录）
- 进程的 PID、PPID、STAT 列分别代表什么
- `grep` 的基本正则：`^`、`$`、`.`、`*`、`[0-9]`

**产出**：能创建用户、查看其所属组；能在 `/var/log/auth.log` 中找出所有失败登录记录。

---

### 第3周：网络基础——TCP/IP 与抓包

**目标**：理解三次握手/四次挥手，能用 Wireshark 验证。

> **重要调整**：本周聚焦协议本身，暂不引入 nmap 扫描。先看懂正常流量，再学攻击流量。

**动手清单**：
1. 画一张 TCP 三次握手时序图（纸上画，不要抄，画完对照 wiki 验证）
2. `nc -vz <靶机IP> <端口>` 发起连接，Wireshark 抓包，在抓包中找出 SYN/SYN-ACK/ACK
3. `curl http://example.com` 同时抓包，观察 HTTP 请求在 TCP 流中的位置
4. 用 `dig <域名>` 查看 DNS 查询过程，理解 A/MX/TXT 记录

**重点理解**：
- TCP 标志位：SYN、ACK、FIN、RST 各自的作用
- 为什么是三次握手而不是两次或四次？（防历史连接）
- ICMP 报文结构中哪些字段可以被隧道利用（Data 字段可填充任意内容）
- **面试高频**：OSI 七层与 TCP/IP 四层的对应关系

**产出**：能在 Wireshark 中指认三次握手的每一步及标志位变化。

---

### 第4周：Shell 脚本基础 + 计划任务 + 日志系统

**目标**：写出第一个可运行的安全脚本。

**动手清单**：
1. 编写一个最简单的脚本 `hello.sh`：`#!/bin/bash` + `echo` + `chmod +x` + 执行
2. 学习变量、条件判断（`if [ ]`）、循环（`for`、`while`）、`grep` + `awk` 组合
3. `crontab -e` 添加一条每 5 分钟写时间戳到 `/tmp/alive.log` 的任务
4. 用 `curl` 伪造 10 次错误登录请求，同时 `tail -f /var/log/auth.log` 观察日志变化
5. 用 `awk`+`sort`+`uniq -c` 从模拟的 access.log 中统计 404 请求来源 IP 排名

**重点理解**：
- 脚本的 `#!` shebang 是什么
- crontab 五个时间字段的格式：分 时 日 月 周
- `awk '{print $1}'` 提取 IP、`sort | uniq -c | sort -rn` 统计排名——这条管道链是安全运维日活

**产出**：一条可运行的 crontab；能从日志中统计 IP 频次排名。

---

### 第5周：iptables 与主机防火墙

**目标**：理解包过滤防火墙的工作原理，能用 iptables 写规则。

> **调整**：原计划 GNS3 + iptables 同期进行，现拆分——本周只做 iptables，GNS3 放到大二再补。
> 理由：GNS3 学习曲线陡峭，且安全运维面试几乎不考网络设备配置，主机防火墙是核心。

**动手清单**：
1. 查看当前 iptables 规则：`iptables -L -n -v`
2. 添加一条禁止特定 IP 访问 22 端口的规则，从另一台虚拟机验证
3. 添加白名单规则：只允许自己的 IP 访问 SSH
4. 理解 `iptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT` 为什么必须放在规则链前面
5. 对比实验：ACL 在网络层拦截 vs iptables 在主机层拦截——能口述区别即可，不需要 GNS3 实操

**重点理解**：
- iptables 的三表五链：filter(INPUT/OUTPUT/FORWARD)、nat、mangle
- DROP vs REJECT 的区别（DROP 静默丢弃，REJECT 会回复 ICMP 不可达）
- 入站规则 vs 出站规则：入站防攻击者进来，出站防反弹 Shell 出去

**产出**：能写出限制 SSH 访问的 iptables 规则并在两台虚拟机间验证。

---

### 第6周：SSH 封禁脚本 v1.0（两周核心项目）

**目标**：从零编写一个可运行的 SSH 暴力破解自动封禁脚本。

> **调整**：原计划这个项目在第5-6周，实际需要 1.5-2 周独立完成。这是你简历上的第一个核心项目，值得花时间打磨。

**脚本功能要求**：
1. 读取 `/var/log/auth.log` 中 "Failed password" 记录
2. 提取来源 IP，统计失败次数
3. 超过阈值（默认 5 次）的 IP 调用 iptables 封禁
4. 添加白名单（至少包括自己的 IP 和 127.0.0.1）
5. 1 小时后自动解封
6. 每次封禁/解封写入 `/var/log/auto-ban.log`，带时间戳
7. 脚本加入 crontab，每 5 分钟执行一次

**开发步骤（不要跳步）**：
1. 先用 grep 手工提取日志 → 理解数据源
2. 写一个只统计不封禁的版本 → 验证逻辑正确
3. 加入 iptables 封禁命令 → 先在测试机验证
4. 加入解封逻辑和时间窗口 → 用 `at` 或时间戳文件
5. 加上白名单和日志记录 → 防止把自己踢出去
6. 部署到 crontab → 用另一台机器 hydra 模拟攻击验证

**产出**：可运行的脚本 + GitHub 仓库 `security-scripts/` 目录。

---

### 第7周：Git 基础 + 仓库初始化 + 期中整理

**目标**：学会 Git 基本操作，把前 6 周的成果整理到 GitHub。

> **重要补充**：原计划没有 Git 基础教学，但大量提到 GitHub。这周补上。

**动手清单**：
1. `git init`、`git add`、`git commit`、`git remote add origin`、`git push`
2. 创建 `.gitignore`，确保私钥、密码、配置文件中的敏感信息不会被上传
3. 安装 git-secrets 或配置 pre-commit hook 扫描敏感信息
4. 初始化仓库结构：

```
Security-Ops-Homelab/
├── README.md         # 仓库概述、学习目标、环境说明
├── notes/            # 每周学习笔记（从第1周迁移过来）
├── scripts/          # SSH封禁脚本等
├── configs/          # iptables规则备份等
└── images/           # 实验截图
```

5. 把前 6 周的笔记整理为 `notes/week01-linux-filesystem.md` 等文件提交
6. 学会 `git log`、`git diff`、`git checkout -b` 分支操作

**重点理解**：
- commit 粒度：不要攒一周一次性提交，每完成一个小目标就 commit
- commit message 规范：用动词开头，"add SSH ban script v1.0" 而不是 "update"
- 绝不提交的内容：私钥（id_rsa）、密码明文、带 token 的配置文件

**产出**：GitHub 仓库上线，包含前 6 周的所有笔记和脚本。

---

### 第8–10周：期末考试 + 轻量维护

**目标**：平稳度过期末考试，保持手感不中断。

> **重要调整**：原计划在考试周还安排了端口镜像实验，不现实。考试期间只做维护。

**轻量任务（每天 ≤ 30 分钟）**：
- 每天 15 分钟回顾一篇自己的笔记
- 每周整理一次本周遇到的命令技巧到对应笔记文件
- 在手机或平板上阅读安全资讯（推荐：FreeBuf、安全客），遇到不懂的名词记下来，考完再查
- 如果有余力：做 2-3 道 CTFShow Misc 题保持兴趣

**明确不做**：
- 不新增实验
- 不安装新软件
- 不开新坑

---

## 二、暑假阶段（7月13日–8月31日，共7周）

### 硬件环境准备

暑假开始前，确认以下环境就绪：

| 虚拟机 | 系统 | 内存 | 用途 |
|--------|------|------|------|
| VM1 | Ubuntu Server 22.04 | 2GB | Web 服务器（Nginx+MySQL+PHP） |
| VM2 | Ubuntu Server 22.04 | 4GB | 日志平台（Graylog） |
| VM3 | Kali Linux | 2GB | 攻击机 |
| VM4（可选） | Ubuntu Server 22.04 | 2GB | Suricata 分析机 |

> 如果内存不足，VM4 可以省略，Suricata 直接装在 Web 服务器上。VM1 和 VM4 共 4GB 即可。

### 每日节奏

- 14:00–16:00：高专注动手（2 小时，中间不刷手机）
- 16:00–17:00：弹性时间（整理文档 / 补漏 / 自由探索 / 休息）
- 晚上可选：30 分钟回顾，不强制
- 周日：休息（完全脱离，不碰电脑）

> 学车、家务等用碎片时间处理，不打断主线。但学车当天下午精力会打折，允许只做 1 小时。

---

### 第1周（7/13–7/19）：Python 基础速成

> **重要补充**：原计划完全没有 Python，这是最关键的补入。
> 安全运维的第一生产力工具是 Python，不是 Shell。Shell 用来做简单任务，Python 用来做复杂逻辑。

**目标**：不是"学会 Python"，而是"能用 Python 处理日志和自动化"。

**Day 1–2：基础语法**
- 变量、列表、字典、if/for/while、函数定义
- 文件读写：`open()`、`readlines()`、`with` 语句
- 字符串操作：`split()`、`strip()`、`startswith()`

**Day 3–4：日志处理专项**
- `re` 模块：用正则提取 IP、时间戳、状态码
- `subprocess` 模块：在 Python 中调用系统命令
- `argparse`：让脚本接受命令行参数
- `csv`/`json` 模块：输出结构化数据

**Day 5：实战——用 Python 重写日志分析**
- 读取 access.log，统计 404 请求来源 IP 排名，输出 CSV
- 读取 auth.log，提取 "Failed password" 记录，输出 JSON
- **和之前 Shell 版本对比**：哪些场景 Python 更好用？（复杂逻辑、数据格式转换、API 调用）

**Day 6–7：缓冲区 + 自由探索**
- 用 Python 写一个简单的端口扫描器（`socket` 模块）
- 或用 Python 调用 Shodan API 查询 IP 信息
- 选一个方向深挖，不贪多

**产出**：至少 3 个可运行的 Python 脚本上传 GitHub。

---

### 第2周（7/20–7/26）：Graylog 部署 + LNMP 环境搭建

**目标**：搭建安全运维的核心平台——集中日志管理 + Web 服务。

> **调整**：原计划一周同时完成 Graylog + LNMP + 全线加固，不现实。
> 现拆分为：本周只搭建和跑通，加固放到下周。

**Graylog 部分（预计 2-3 天）**：
1. 优先使用 Graylog 官方 OVA 虚拟机——省去所有安装折腾
2. 如果自己装：严格按照 Graylog 官方文档的版本匹配表，Java + MongoDB/OpenSearch + Elasticsearch + Graylog 四个组件版本必须兼容
3. 配置 rsyslog 将 Web 服务器的系统日志转发到 Graylog
4. **验收**：在 Graylog Web 界面中能搜索到来自 Web 服务器的 sshd 和 auth 日志

**LNMP 部分（预计 2 天）**：
1. 在 VM1 上安装 Nginx + MySQL + PHP，搭建一个简单的 Web 页面（哪怕只显示 "Hello Security"）
2. 配置 Nginx 日志格式为 JSON（方便 Graylog 解析）
3. 配置 Nginx 访问日志和错误日志通过 rsyslog 发送到 Graylog
4. **验收**：访问 Web 页面，在 Graylog 中能看到对应的访问日志

**如果 Graylog 实在折腾不过去（超过 1 天半）**：
- 降级方案：用 `goaccess` 做 Web 日志可视化 + `lnav` 做系统日志查看
- 不要卡进度，工具为你服务

**产出**：Graylog 中可见 Web 和系统日志；Web 服务可访问。

---

### 第3周（7/27–8/2）：LNMP 加固 + 自动防御脚本移植

**目标**：完成一台 Web 服务器的基线加固，移植封禁脚本。

**加固清单（逐项执行并验证）**：

**MySQL 加固**：
- 删除匿名用户和 test 库
- root 仅允许 localhost 登录
- 为应用创建独立低权限用户

**Nginx 加固**：
- 隐藏版本号：`server_tokens off`
- 限制请求方法：只允许 GET/POST/HEAD
- 关闭目录浏览：`autoindex off`
- 限制连接数和请求速率：`limit_conn_zone` + `limit_req_zone`

**PHP 加固**：
- 禁用危险函数：`exec`、`system`、`passthru`、`shell_exec`、`popen`、`proc_open`
- `cgi.fix_pathinfo=0`
- `expose_php=Off`

**iptables 加固**：
- 仅开放 22/80/443，入站默认 DROP
- 添加出站限制：至少限制 MySQL 端口（3306）不对外监听，限制外发 DNS（只允许到指定 DNS 服务器）
- 保留一条 `ESTABLISHED,RELATED` 放行规则

**加固验证**：
- 运行 `lynis audit system`，针对未达标项修正
- 运行一遍修正前后对比，记录分数变化

**脚本移植**：
- 将第 6 周的 SSH 封禁脚本部署到 Web 服务器
- 新增 Web 暴力破解监控脚本：扫描 access.log 中 401/403，统计 IP 并联动 iptables
- 白名单务必加上自己的 IP 和内网管理段
- 封禁/解封事件通过 syslog 转发到 Graylog

**产出**：lynis 加固前后对比截图；两个封禁脚本稳定运行；Graylog 中可见封禁事件。

---

### 第4–5周（8/3–8/16）：4个核心漏洞攻防转换（核心跃升）

> **调整**：原计划 2 周覆盖 5 个漏洞 + SSRF。砍掉 SSRF（大一理解门槛太高），保留 4 个核心漏洞。SQL 注入周前两天补 SQL 基础。
> 每个漏洞用 2 天深挖，节奏是：攻→防→溯源→文档。

**漏洞列表（按学习顺序）**：

| 顺序 | 漏洞 | 理由 |
|------|------|------|
| 1 | 文件上传 | 攻击链最简单，适合热身 |
| 2 | 命令注入 | 直接体验"代码执行"的危害 |
| 3 | SQL 注入 | 需先补 SQL 基础（前 1 天） |
| 4 | 存储型 XSS | 理解前端安全的特殊性 |

**每个漏洞执行标准三步闭环**：

**Day A：攻击面**
1. 在 Web 服务器上部署对应漏洞环境（推荐 DVWA 或 sqli-labs）
2. Kali 上手工具攻击（Burp/手工/sqlmap），同时 tcpdump 抓包
3. 在 Wireshark 中标记攻击特征包（SQLi 的 `' OR 1=1--`、XSS 的 `<script>` 等）
4. 理解漏洞根因——不只是"怎么做"，而是"为什么会有这个漏洞"

**Day B：阻断 + 溯源**
1. 安装 ModSecurity + OWASP CRS 规则集
2. 先用 CRS 默认规则验证攻击被拦截（返回 403）
3. 阅读一条 CRS 规则的语法，尝试仿写一条精简版
4. 在 Graylog 中搜索该攻击的日志记录
5. 在 `cases/` 目录下整理一页报告：原理→攻击包→WAF 规则→日志证据→加固建议
6. 报告中必须写一段"误报场景分析"：什么情况下这条规则会误伤正常请求？

**产出**：4 份攻防案例报告（PDF），存放于 `cases/` 目录。

---

### 第6周（8/17–8/23）：Suricata IDS + 红蓝全流程演练

**目标**：部署网络入侵检测，完成一次完整的红蓝对抗并输出处置报告。

**Suricata 部分（前 2 天）**：
1. 在分析机或 Web 服务器上 `apt install suricata`
2. 加载 Emerging Threats Open 规则集
3. 配置监听网卡——如果只有两台机（Kali + Web），Suricata 装在 Web 服务器上监听本地流量
4. Kali 发起 nmap 扫描 + 上一周的漏洞攻击，在 `eve.json` 中确认告警
5. 将 Suricata 告警导入 Graylog（配置 Filebeat 或直接 rsyslog 读取 eve.json）

**红蓝对抗全流程（中间 3 天）**：

**红队视角**：
- 信息收集 → 端口扫描 → 漏洞利用（混合前两周 2-3 种攻击）
- 尝试建立持久化（添加 SSH 公钥后门或 crontab 反弹 Shell）

**蓝队视角（核心）**：
- 同时盯 Graylog 和 Suricata 告警面板
- 观察自动封禁脚本被触发
- 观察 WAF 阻断日志
- 按照 **SANS PICERL 六阶段**记录处置过程：
  1. **准备（Preparation）**：环境已有的防护措施
  2. **识别（Identification）**：哪个告警最先触发？从哪条日志发现攻击？
  3. **遏制（Containment）**：自动封禁是否生效？是否需要手动加固？
  4. **根除（Eradication）**：漏洞根因是什么？如何修复？
  5. **恢复（Recovery）**：清理后门、恢复服务
  6. **总结（Lessons Learned）**：哪里做得不好？如何改进？

**Windows 安全体验（最后 1 天）**：
> 原计划给了 Windows 一整块时间，现简化为 1 天体验。Windows 安全运维是独立方向，大二再系统学。

- 配置本地安全策略：登录审核
- 事件查看器识别 4624/4625/4672/4688/4698 五个事件 ID
- 安装 Sysmon 观察进程创建事件
- 记录排查最小清单：`netstat -ano`、`schtasks`、注册表 Run 项

**产出**：一份按 PICERL 框架编写的《红蓝对抗安全事件处置报告》；Suricata 告警截图；Windows 安全笔记。

---

### 第7周（8/24–8/30）：收尾整合与能力展示

**目标**：将所有成果组织为一个"面试可以直接展示"的 GitHub 仓库。

**仓库最终结构**：

```
Security-Ops-Homelab/
├── README.md              # 架构概述 + 能力清单 + 快速启动指南
├── cases/                 # 4个攻防案例 PDF
├── scripts/               # SSH封禁、Web爆破监控、备份脚本、Python日志分析
├── configs/               # Nginx、ModSecurity、iptables、Suricata 配置备份
├── incident-response/     # 红蓝演练处置报告
├── notes/                 # 每周学习笔记（按周编号）
├── images/                # 拓扑图、实验截图、lynis 报告截图
└── docker-compose.yml     # （加分项）一键拉起环境
```

**本周具体任务**：
1. 用 draw.io 画出完整网络拓扑图，标注所有主机 IP、服务、流量方向
2. README 必须包含：
   - 构建目的（一句话：我搭建这个环境是为了验证安全运维的全流程能力）
   - 架构说明（拓扑图 + 各组件职责）
   - 一次模拟攻击的闭环流程图（攻击发起 → WAF 拦截 → 日志记录 → Graylog 告警 → 脚本封禁 → 处置报告）
   - 已具备的安全运维能力清单（每项能力对应仓库中的哪个目录/文件）
   - 快速启动指南：别人拿到仓库后如何复现（vagrant up / docker-compose / 手动部署文档）
3. 所有脚本添加 README 注释头：用途、用法、依赖
4. 所有配置文件添加注释说明每行规则的含义
5. 给 week 1-6 的学期笔记做最终整理，确保格式统一
6. 检查 `.gitignore`，确认没有任何敏感信息被提交

**可选加分项**：
- 录制 3 分钟演示视频：发起攻击 → WAF 阻断 → 日志告警 → 自动封禁
- 写一篇技术博客发布到掘金/CSDN

---

## 三、暑假结束验收标准

在暑假最后一天，你应该能：

- [ ] 30 分钟内从 SSH 登录开始完成一台裸机 Web 服务器的基线加固
- [ ] 面对一条攻击请求，口述它在自己环境中将在哪些节点产生何种日志
- [ ] 独立写出一个 Python 或 Shell 脚本来分析日志并输出统计结果
- [ ] 口述 SQL 注入、文件上传、命令注入、XSS 四种漏洞的原理和修复方案
- [ ] 按照 PICERL 六阶段讲清楚一次安全事件的处置流程
- [ ] 说出 4624/4625/4688 三个 Windows 事件 ID 的含义
- [ ] GitHub 仓库结构清晰，README 能让一个陌生人看懂你在做什么

---

## 四、大二及以后的衔接建议

### 大二上学期（2025.9–2026.1）

- **Python 进阶**：requests 库、Flask 写简单 API、与现有 homelab 集成
- **Wazuh（HIDS）部署**：整合到现有环境，和 Graylog + Suricata 形成三位一体的检测体系
- **SRC 实战**：在漏洞响应平台（补天、漏洞盒子）提交合法漏洞，积累真实漏洞经验
- **开始写博客**：把 homelab 的搭建过程写成系列文章
- **蹭一次秋招面试**：哪怕不过，目的是提前了解面试官问什么

### 大二下学期（2026.2–2026.7）

- **Docker 化整个 homelab**：写 docker-compose.yml，一键部署所有环境
- **ELK 技术栈深入学习**：Elasticsearch 查询语法、Kibana 仪表盘
- **护网行动蓝队面试**：用你的 GitHub 仓库和案例展示能力
- **拿下 RHCSA 或 Security+ 认证**（可选）：简历筛选阶段有证书是加分项

### 大三（2026.9–2027.7）

- **全力找安全运维实习**：你的 GitHub 仓库 + 博客 + 可能的证书 > 大多数同届同学
- **护网蓝队实战**：实习或护网二选一，有真实生产环境经验后秋招直接转化
- **秋招投递**：安全运维、SOC 分析师、安全运营工程师三个方向同时投

---

## 附录：面试高频知识点对照表

| 知识点 | 在本计划的覆盖 | 优先级 |
|--------|---------------|--------|
| Linux 基础操作与权限模型 | 第1-2周重点覆盖 | ★★★★★ |
| 常用命令与文本处理（grep/awk/sed） | 第2、4周覆盖 | ★★★★★ |
| TCP/IP 协议与 OSI 模型 | 第3周覆盖 | ★★★★☆ |
| HTTP 协议与状态码 | 第3周入门，暑假攻防深化 | ★★★★☆ |
| SQL 基础与 SQL 注入原理 | 暑假第4-5周覆盖 | ★★★★☆ |
| OWASP Top 10 | 覆盖4项（缺少CSRF、XXE等） | ★★★☆☆ |
| Python 脚本能力 | 暑假第1周入门 | ★★★★☆ |
| Shell 脚本能力 | 第4、6周覆盖 | ★★★★☆ |
| iptables/防火墙 | 第5周覆盖 | ★★★☆☆ |
| 日志分析（SOC 核心能力） | 贯穿全计划 | ★★★★★ |
| 应急响应流程 | 暑假第6周 PICERL | ★★★☆☆ |
| WAF 原理（ModSecurity） | 暑假第4-5周 | ★★★☆☆ |
| IDS/IPS（Suricata） | 暑假第6周 | ★★☆☆☆ |
| Windows 安全基础 | 暑假第6周（1天体验） | ★★☆☆☆ |
| Docker/容器 | 未覆盖（建议大二补） | ★★☆☆☆ |
| 加密基础（对称/非对称/哈希） | 未覆盖（建议碎片时间自学） | ★★★☆☆ |

---

> **最后提醒**：计划是地图，不是铁轨。如果某周的任务提前完成，不要急着赶进度——深挖当前方向，把理解做深比把列表刷完重要十倍。如果某周卡住了，标记问题、继续前进，不要在一棵树上吊死。大二回头看这份计划时，你可能会嘲笑现在的自己"太天真"——那是好事，说明你成长了。
